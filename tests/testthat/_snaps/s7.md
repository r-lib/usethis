# ensure_s7_methods_register() prompts if file differs from template

    Code
      ensure_s7_methods_register()
    Message
      [ ] Ensure `S7::methods_register()` is called in `.onLoad()` in 'R/zzz.R'.

