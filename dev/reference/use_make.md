# Create Makefile

`use_make()` adds a basic Makefile to the project root directory.

## Usage

``` r
use_make()
```

## See also

The [documentation for GNU
Make](https://www.gnu.org/software/make/manual/html_node/).
