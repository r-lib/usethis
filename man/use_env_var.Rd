% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edit.R
\name{use_env_var}
\alias{use_env_var}
\title{Set an environment variable in \code{.Renviron}}
\usage{
use_env_var(name, value = NULL, scope = NULL)
}
\arguments{
\item{name}{Name of the environment variable. Must contain only letters,
digits, and underscores, and must start with a letter or underscore.}

\item{value}{Value to set. If \code{NULL} (the default), you are prompted to
enter the value securely using \code{\link[askpass:askpass]{askpass::askpass()}}.}

\item{scope}{Edit globally for the current \strong{user} (\code{"user"}), or locally
for the current \strong{project} (\code{"project"}). Defaults to \code{"project"} when
an active project is detected, otherwise \code{"user"}.}
}
\value{
Path to the \code{.Renviron} file, invisibly.
}
\description{
Adds or updates an environment variable in \code{.Renviron} and immediately
makes it available in the current session via \code{\link[=Sys.setenv]{Sys.setenv()}}. The
\code{scope} defaults to \code{"project"} when an active project is detected,
otherwise \code{"user"}.
}
\examples{
\dontrun{
use_env_var("OPENAI_API_KEY")
}
}
